package src

func (b *Board) checkCrossFillWithOneSectorAndBlockedSegment() string {
	//for _, c := range solvedCells(b.Cells) {
	// Get Sector x,y

	// Try Horizontal First
	// we loop through the other 2 sectors and find all possible values if there one of the segments only has possible cells in one row, we remove all available cells from the otehr sector

	// Get horizontally related sectors
	//}
	return ""
}
